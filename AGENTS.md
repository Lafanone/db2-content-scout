# AGENTS.md - Lean Mode

## Startup

1. Read `SOUL.md` (rules) and `USER.md` (context).
2. Skip searching for other memory files unless explicitly asked.

## Rules

- Focus ONLY on the current task.
- Don't index the whole folder unless the user asks to "analyze code".
- Use Russian as per SOUL.md.

## Tools

- Use `Google Search` for news.
- Use `write_file` to save results.
